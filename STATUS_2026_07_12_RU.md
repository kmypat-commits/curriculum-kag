# Статус Curriculum-KAG — 2026-07-12

## Сделано

- Проиндексированы:
  - `curriculum-kag`: 1588 узлов / 3952 связи;
- Исправлен `start.ps1`: fallback PostgreSQL → SQLite теперь проверяет не только `backend/.env`, но и корневой `.env`.
- Исправлены одинаковые A/B/C для малых программ: после NSGA-II применяется безопасная диверсификация B/C по same-credit дисциплинам внутри доменов проекта.
- Исправлен ответ `/planner/{version}/build`: теперь сразу возвращается `semester_lo_details`, поэтому блок «Результаты обучения семестра» заполняется без повторной загрузки вариантов.
- Усилен EPVO-фильтр направлений: `/epvo/directions` теперь одновременно проверяет уровень образования (`6B/7M/8D`) и выбранную область образования.
- Улучшено ранжирование EPVO-кандидатов для генерации: теперь учитываются группа/направление, смысловая близость к названию/цели/доменам/LO программы, частотность в ЕПВО и наличие типовых кредитов/семестра.
- Новым EPVO-дисциплинам домен назначается по смысловой близости к доменам проекта, а не простым чередованием.
- Экран «Сравнить с ЕПВО» расширен:
  - процент совпадения с типовыми дисциплинами ЕПВО;
  - похожие программы ЕПВО;
  - типовые LO из реальных карточек;
  - слабые места и рекомендации;
  - релевантность типовых дисциплин к цели/LO проекта.
- Расширена объяснимость графа «Почему выбрана дисциплина?»:
  - `why_selected` с ролью дисциплины, причиной семестра, доменом, LO-count, top-LO;
  - пререквизиты и постреквизиты в карточке дисциплины;
  - evidence details из `MatchScore.evidence_json`;
  - фрагмент `CourseChunk`, если score связан с chunk;
  - последнее экспертное подтверждение/замечание из `MatchFeedback`;
  - UI-карточка графа показывает эти данные и кнопки экспертной обратной связи.

## Проверено

- Backend: `http://127.0.0.1:8000/health` — healthy.
- Frontend/proxy: `http://127.0.0.1:3001/api/health` — healthy.
- Контрольный проект 6 «Цифровое государственное управление» пересобран:
  - A/B/C различаются;
  - 60 кредитов;
  - дубликаты: 0;
  - нарушения пререквизитов: 0;
  - чужие домены: 0;
  - `semester_lo_details`: семестр 1 — 9 LO, семестр 2 — 3 LO.
- EPVO endpoints:
  - `education-areas?education_level=bachelor`: 12 областей;
  - `directions?education_level=bachelor&education_area=6B06`: 3 направления (`6B061`, `6B062`, `6B063`);
  - защита от несоответствия уровня и области работает: `master + 6B06` возвращает 0 направлений.
- `/epvo/compare/6` возвращает:
  - 60 типовых дисциплин;
  - 8 похожих программ;
  - 12 типовых LO;
  - weak spots и рекомендации.
- Frontend build: OK.
- Объяснимость графа:
  - `backend/app/api/planner.py` compile: OK;
  - `frontend` build: OK;
  - backend health: OK.

## Ограничение текущей сессии

- Перезапуск backend и часть финальных регрессий были заблокированы лимитом approvals Codex. Код собран и проверен статически, но новый graph API появится в работающем приложении после ручного перезапуска `stop.bat` → `start.bat` или после восстановления лимита approvals.
- `capture_baseline.py --no-backup`:
  - `projects=11`;
  - `projects_with_non_distinct_abc=0`;
  - `plans_with_duplicates=0`;
  - `plans_with_prerequisite_violations=0`;
  - `plans_with_foreign_domains=0`.
- `audit_localization.py`:
  - RU/KK/EN keys: `316/316/316`;
  - missing: `0`;
  - mojibake: `0`;
  - hardcoded English: `1` — известный UI false-positive.

## Риски

- Корневой `.env` сейчас указывает на PostgreSQL. Если PostgreSQL не запущен, локальный launcher должен уходить на SQLite fallback.
- Старый `IMPLEMENTATION_STATUS_RU.md` отображается в PowerShell как mojibake; чтобы не испортить кодировку, свежий статус вынесен в этот UTF‑8 файл.
