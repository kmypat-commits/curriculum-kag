# Проверка секретов перед GitHub — 2026-08-01

Проверка текущего индекса Git выполнена без вывода значений секретов.

- Отслеживаемых `.env` нет; в Git находится только `.env.example`.
- Реальных ключей в рабочем коде не обнаружено: найденные совпадения относятся к именам настроек, чтению переменных окружения, mock/seed-коду и документационным примерам.
- В Git остаётся один файл больше 50 МБ: `backend/data/course_translations.json` (около 55,5 МБ). Перед публикацией нужен Git LFS или отдельный versioned dataset release.
- Pre-commit hook блокирует добавление `.env` и файлов больше 50 МБ.

Перед первым push необходимо повторить сканирование уже staged-изменений и проверить историю Git, потому что текущий отчёт не является гарантией отсутствия секретов в старых коммитах.
