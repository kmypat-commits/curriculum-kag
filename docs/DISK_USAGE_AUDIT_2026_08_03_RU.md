# Аудит диска проекта

Проверка выполнена 2026-08-03 без удаления файлов.

| Каталог | Размер | Назначение | Решение |
|---|---:|---|---|
| `backend/models` | 49.57 GB | SBERT checkpoints и базовые веса | сохранить production candidate `epvo-sbert-finetuned-40k`; остальные сначала сверить с manifest, затем архивировать отдельно |
| `backend/models/paraphrase-multilingual-mpnet-base-v2` | 10.11 GB | базовая multilingual модель | сохранить как reproducibility dependency |
| `.runtime/models` | 4.63 GB | временные/экспериментальные артефакты | не удалять до сверки experiment manifests |
| `.runtime/epvo-weighted-postgres` | 3.41 GB | экспорт/эксперимент ranking | архивировать после фиксации metrics |
| `backups/postgres` | 2.11 GB | резервные копии PostgreSQL | оставить минимум последнюю проверенную и одну предыдущую |
| `frontend/node_modules` | 0.13 GB | локальные зависимости | не включать в Git; пересоздаётся через lockfile |

## Безопасный порядок очистки

1. Составить manifest для каждой модели и эксперимента: путь, SHA-256, metrics, дата, production/experimental статус.
2. Проверить, что `epvo-sbert-finetuned-40k` и базовая модель воспроизводимо загружаются.
3. Переместить устаревшие checkpoints в внешний архив, не удаляя их сразу.
4. После независимой проверки восстановления удалить только подтверждённые дубликаты.

Немедленное удаление не выполнялось: часть артефактов нужна для воспроизводимости диссертации и статьи.
