# Статус Curriculum-KAG на 2026-07-26

## Коротко

Система сейчас запускается и проверяется в режиме PostgreSQL shadow:

```powershell
.\start.ps1 -Database postgres-shadow
```

Это не окончательное переключение “PostgreSQL как единственная основная БД”, но уже рабочий безопасный режим: приложение читает и пишет в PostgreSQL shadow, а SQLite остаётся rollback-точкой.

## Проверено на live-сервисе

- Backend health: `200`.
- Frontend health через proxy: `200`.
- Активная БД по фактическим данным: PostgreSQL shadow.
- Контрольный признак PostgreSQL shadow: план `plan_id=600` доступен в live API.
- `/repository/stats` после кэша:
  - первый запрос: около `0.327 s`;
  - повторный запрос: около `0.008 s`;
  - дисциплин в репозитории: `20782`.

## PostgreSQL shadow

Миграция SQLite → PostgreSQL shadow выполнена. Валидная сверка сохранена:

```text
.runtime/sqlite-postgres-counts-valid.json
```

Сверка валидных строк прошла успешно. Старые orphan-строки из SQLite не переносились, потому что PostgreSQL строже проверяет внешние ключи.

## Качество текущего контрольного плана

Контрольный проект: `project_version_id=15`.

| Вариант | Активный | Кредиты | Bridge | Сомнительные дисциплины | ГОСО РК | International quality |
|---|---:|---:|---:|---:|---:|---:|
| A | нет | 240 | 2 | 0 | OK | 66.7% |
| B | нет | 240 | 3 | 0 | OK | 66.7% |
| C | да | 240 | 2 | 0 | OK | 100.0% |

Вывод: активный вариант C сейчас является лучшим контрольным планом: правильная сумма кредитов, ГОСО проходит, сомнительных дисциплин нет, international quality = 100%.

## Что улучшено в этой сессии

- Стабилизирована страница “Версии проекта”.
- Git API работает с `safe.directory`, чтобы Windows/Git не ломал статус репозитория.
- Исправлен парсинг путей изменённых файлов в Git status.
- Добавлено понятное объяснение в блоке “Почему выбрана?”:
  - `итог` — финальная сила связи дисциплина → LO;
  - `ИИ` — прогноз текстовой модели;
  - `ЕПВО` — экспертная поддержка из базы;
  - проценты не складываются.
- Если после генерации A/B/C нет активного плана, backend автоматически активирует лучший вариант.
- Массовая замена bridge теперь реально меняет текущий план, а не только сохраняет замену на будущее.
- Для сомнительных дисциплин добавлены понятные причины и рекомендации.
- Добавлен кэш `/repository/stats` на 60 секунд.
- Fallback загрузки страниц стал понятнее: пользователь видит, что большие графы/отчёты могут открываться несколько секунд.
- Страница графа оптимизирована: основной chunk `PrerequisiteGraph` уменьшен примерно с `506 KB` до `17 KB`; `cytoscape` и `cytoscape-dagre` теперь догружаются лениво.
- Проверены graph API на всех 5 проектах: графы отвечают примерно за `0.05–0.10 s`, компетенции семестров — примерно за `0.2–0.4 s`, циклов не обнаружено.
- Уточнена семестровая эвристика сложности: Kafka/MQTT и потоковая обработка не допускаются в 1 семестр, но базовые дисциплины вроде “Алгоритмы и структуры данных” больше не помечаются ошибочно как слишком ранние из-за устаревшего `domain`.

## Готовность по критериям

| Критерий | Оценка | Комментарий |
|---|---:|---|
| Запуск локально | 85% | Работает, но `start.ps1` в Codex иногда выглядит как `aborted`, хотя сервис поднимается. |
| SQLite rollback | 90% | SQLite остаётся резервной точкой. |
| PostgreSQL shadow | 85% | Миграция и live-smoke прошли; Docker CLI из sandbox иногда требует отдельного разрешения. |
| PostgreSQL как основной backend | 60% | Технически близко, но ещё нужен финальный cutover-план и rollback-команда. |
| Генерация планов A/B/C | 80% | Генерация стала стабильнее, лучший вариант активируется автоматически. |
| Качество контрольного плана | 85% | Вариант C даёт 100% international quality на контрольном проекте. Нужно проверить больше направлений. |
| ГОСО РК | 80% | Обязательные дисциплины защищены; текущий контрольный план проходит ГОСО. |
| ЕПВО-репозиторий | 80% | 20782 утверждённых дисциплины в active repository, raw/normalized слои сохранены. |
| Bridge replacement | 75% | Есть одиночная и массовая замена; нужна дальнейшая проверка на разных ОП. |
| Объяснимость LO | 75% | Объяснения стали понятнее; нужно ещё унифицировать все панели анализа. |
| Производительность UI | 65% | API быстрые, но графовый chunk около 506 KB и требует дальнейшего code-splitting. |
| Продакшн-готовность | 60% | Сильный локальный прототип/предпродакшн, но не финальный production. |

Обновление после оптимизации графа:

| Критерий | Оценка | Комментарий |
|---|---:|---|
| Производительность UI | 75% | Графовая страница стала лёгкой на входе; тяжёлая библиотека грузится отдельно. Остаётся оптимизировать runtime-отрисовку больших графов и часть общих chunks. |
| Семестровая логика сложных дисциплин | 80% | Kafka/MQTT удерживается от ранних семестров; ложные предупреждения по базовым дисциплинам исправлены. |
| PostgreSQL cutover readiness | 85% | Readiness script показывает `ready_for_cutover=true`; до production нужно сделать свежий restore-point и ручной UI-smoke. |

## Kafka/Big Data

Kafka/MQTT используется в системе как учебная Big Data / stream-processing дисциплина, а не как runtime-инфраструктура самого приложения. Алгоритм учитывает её как сложную дисциплину: она не должна ставиться в первый семестр, потому что требует базовых знаний по программированию, сетям, данным и архитектуре систем.

## Следующие шаги

1. Подготовить аккуратный cutover PostgreSQL shadow → PostgreSQL primary:
   - backup SQLite;
   - backup PostgreSQL shadow;
   - финальная сверка counts;
   - явная rollback-команда.
2. Проверить генерацию на 3–5 разных направлений:
   - бакалавриат;
   - магистратура;
   - докторантура;
   - ГОСО РК;
   - международный режим.
3. Оптимизировать тяжёлую страницу графа:
   - вынести тяжёлые части графа в отдельные lazy-компоненты;
   - добавить progress/empty-state;
   - проверить время открытия в браузере.
4. Доработать “Сомнительные дисциплины”:
   - массовое подтверждение/замена;
   - понятная маркировка “ГОСО защищено”;
   - пересчёт метрик после замены.
5. Продолжить научный трек:
   - Recall@10 ranking experiment;
   - SBERT/reranking сравнение;
   - оформить результаты для статьи.
