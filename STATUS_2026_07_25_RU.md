# Статус Curriculum-KAG на 25.07.2026

## Сделано

- Зафиксирована быстрая точка отката SQLite: `archive/backups_2026-08-01/baseline-2026-07-25_fast`.
- Проверены ключевые объёмы SQLite: база около 11.45 GB, нормализованных дисциплин ЕПВО 191 292, экспертных связей discipline–LO 932 483.
- Добавлен безопасный shadow-compose для PostgreSQL/pgvector: `docker-compose.postgres-only.yml`.
- Обновлена инструкция перехода: `docs/POSTGRESQL_MIGRATION_RU.md`.
- Добавлен post-migration контроль counts: `backend/scripts/compare_sqlite_postgres_counts.py`.
- Docker Desktop найден в пользовательской установке `C:\Users\User\AppData\Local\Programs\DockerDesktop`.
- Docker CLI работает при добавлении `resources\bin` в PATH.
- Shadow PostgreSQL/pgvector контейнер `curriculum-kag-postgres-shadow` поднят на порту `5433`, состояние healthy, расширение `vector` проверено.
- Ускорен экран “Сравнить с ЕПВО”: уменьшена тяжёлая первичная выборка, добавлено время расчёта `elapsed_seconds`, сохранён TTL-кэш.
- Переписан `frontend/src/pages/EpvoComparison.jsx` чистым UTF-8: убраны битые русские/казахские строки, добавлены понятные сообщения о лёгком режиме и прогрессе.
- Ускорен фильтр “Репозиторий дисциплин” по направлению/группе: вместо выгрузки всех `approved_course_id` в Python используется SQL-subquery.
- В Plan Builder ручной выбор A/B/C больше не сбрасывается при обновлении вариантов.
- В LO Coverage добавлен индикатор обновления при смене варианта и время расчёта источников покрытия.
- Свежая frontend-сборка скопирована в `.runtime/dist`, который отдаёт локальный сервер `localhost:3001`.
- Ускорены preview-запросы Plan Builder для bridge/course replacements: добавлено раннее SQL-ранжирование и лимиты кандидатов, чтобы не загружать все MatchScore в Python.
- В Plan Builder теперь видно время расчёта bridge/course replacement preview.
- Добавлены и применены SQLite performance indexes для горячих путей:
  - `match_scores(project_version_id, course_id, score)`;
  - `match_scores(project_version_id, lo_id, score)`;
  - `plan_items(plan_id, course_id)`;
  - `plan_items(plan_id, bridge_module_id)`;
  - `plans(project_version_id, variant_type, is_active, id)`;
  - `raw_epvo_learning_outcomes(program_source_id)`.

## Проверено

- `py_compile` для PostgreSQL/backup/readiness/compare-скриптов — успешно.
- Быстрый SQLite preflight — успешно.
- Документ PostgreSQL сохранён в UTF-8 без mojibake.
- `backend/app/api/epvo.py` и `backend/scripts/compare_sqlite_postgres_counts.py` компилируются.
- `backend/app/api/repository.py` компилируется.
- `backend/app/api/planner.py` компилируется.
- `backend/scripts/ensure_sqlite_performance_indexes.py` компилируется и выполнен успешно за ~10 секунд.
- Frontend production build — успешно.

## Риск

- Фактический перенос SQLite → PostgreSQL пока не запускался; Docker уже починен, следующий шаг — миграция в shadow PostgreSQL.
- В корне `D:\curriculum-kag` есть пустая `.git`-папка, но это не валидный Git-репозиторий. Перед публикацией на GitHub нужно восстановить или заново инициализировать Git.
- `.env` нельзя публиковать; секреты нужно держать вне Git и при необходимости ротировать.

## Ближайшие шаги

1. Перенести SQLite в PostgreSQL-стенд и сверить counts.
2. Запустить backend на PostgreSQL-стенде и сравнить контрольные A/B/C планы через API-аудит.
3. Починить генерацию проекта 15 `Киберследователь`: сейчас rebuild падает с `list.remove(x): x not in list`.
4. Проверить реальные API timings после запуска localhost и продолжить оптимизацию только там, где ещё медленно.
